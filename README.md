# NeuralNetworkCPP

Ётот код определ€ет класс NeuralNetwork дл€ нейронной сети с одним скрытым слоем и реализует методы обучени€ и предсказани€. 
 од загружает набор данных Iris, обучает сеть и сохран€ет ее в файле модели. 
«атем можно снова загрузить сеть из файла модели и использовать ее дл€ предсказани€ класса нового цветка ириса, введенного пользователем.

## √лавные классы, которые € вижу в этом коде:

NeuralNetwork - класс нейронной сети, который хранит веса и логику дл€ пр€мого распространени€ и обратного распространени€ обучени€
std::vector - используетс€ дл€ хранени€ входных, выходных и скрытых слоев данных
std::ifstream - используетс€ дл€ загрузки набора данных Iris из файла
std::ofstream - используетс€ дл€ сохранени€ обученной модели в файл
“аким образом, ключевые классы - NeuralNetwork и стандартные шаблонные классы библиотеки векторов/потоков дл€ ввода/вывода данных. 
 ласс NeuralNetwork описывает основную логику нейронной сети.